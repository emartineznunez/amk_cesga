#The following line should be added to your .bashrc file

module use $HOME/amk-2021/modules

#include in the $PATH variable the following path (either in .bashrc or .bash_profile 
$HOME/amk_tools/scripts
